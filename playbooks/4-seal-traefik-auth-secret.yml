# playbooks/4-seal-traefik-auth-secret.yml
# ===============================================================
# 🔐 Crear y cifrar Secret de autenticación básica para Traefik
# - Genera hash htpasswd (Apache)
# - Renderiza Secret plano
# - Lo cifra con kubeseal para uso en GitOps (ArgoCD)
# ===============================================================

- name: 🔐 Generar y cifrar Secret para Traefik Dashboard
  hosts: localhost
  gather_facts: false

  vars_files:
    - ../vars/main.yml

  vars:
    kubeconfig_path: "/home/victory/.kube/config"
    kubeseal_path: "/usr/local/bin/kubeseal"
    kubectl_path: "/usr/local/bin/kubectl"
    files_dir: "{{ playbook_dir }}/files"
    rendered_secret_path: "{{ files_dir }}/traefik-dashboard-secret.yaml"
    sealed_secret_path: "{{ files_dir }}/traefik-dashboard-sealed.yaml"

  tasks:
    # ✅ Validar herramientas necesarias
    - name: 📦 Validar si kubeseal está instalado
      stat:
        path: "{{ kubeseal_path }}"
      register: kubeseal_check

    - name: ❌ Abortamos si kubeseal no está instalado
      fail:
        msg: "❌ kubeseal no está instalado en {{ kubeseal_path }}. Instálalo antes de continuar."
      when: not kubeseal_check.stat.exists

    - name: 📦 Validar si kubectl está instalado
      stat:
        path: "{{ kubectl_path }}"
      register: kubectl_check

    - name: ❌ Abortamos si kubectl no está instalado
      fail:
        msg: "❌ kubectl no está instalado en {{ kubectl_path }}. Instálalo antes de continuar."
      when: not kubectl_check.stat.exists

    # ✅ Validar credenciales
    - name: ❌ Abortamos si traefik_auth_user o traefik_auth_pass están vacíos
      fail:
        msg: "❌ Variables traefik_auth_user o traefik_auth_pass no están definidas. Exporta o carga .env."
      when: traefik_auth_user | length == 0 or traefik_auth_pass | length == 0

    # 📁 Crear directorio si no existe
    - name: 📁 Crear directorio de archivos renderizados
      file:
        path: "{{ files_dir }}"
        state: directory
        mode: "0755"

    # 🔐 Generar hash htpasswd
    - name: 🔐 Generar hash htpasswd
      command: htpasswd -nb {{ traefik_auth_user }} {{ traefik_auth_pass }}
      register: traefik_htpasswd_output
      no_log: true

    - name: 🔧 Limpiar salto de línea del hash generado
      set_fact:
        traefik_basic_auth: "{{ traefik_htpasswd_output.stdout | trim }}"

    # 📄 Renderizar Secret plano en YAML
    - name: 📝 Renderizar Secret plano en YAML
      template:
        src: ../templates/secrets/traefik-dashboard-secret.yaml.j2
        dest: "{{ rendered_secret_path }}"
      vars:
        traefik_basic_auth: "{{ traefik_basic_auth }}"
      # Elimina la redefinición de 'traefik_namespace' aquí

    # 🔐 Cifrar el Secret con kubeseal
    - name: 🔐 Cifrar el Secret con kubeseal
      shell: >
        {{ kubeseal_path }}
        --controller-name sealed-secrets-controller
        --controller-namespace {{ traefik_namespace }}  # La variable ya está definida y disponible
        --format yaml
        < "{{ rendered_secret_path }}" > "{{ sealed_secret_path }}"
      args:
        executable: /bin/bash
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      changed_when: false