# playbooks/uninstall-traefik-dashboard.yml
# =========================================================
# 🧹 Desinstalación completa y segura de Traefik + recursos
# =========================================================
---
- name: 🧹 Desinstalar Traefik y limpiar recursos relacionados
  hosts: localhost
  gather_facts: false

  vars_files:
    - ../vars/main.yml

  vars:
    kubeconfig_path: "/home/victory/.kube/config"
    helm_bin_path: "/usr/local/bin/helm"
    kubectl_bin_path: "/usr/local/bin/kubectl"
    files_dir: "{{ playbook_dir }}/files"

  environment:
    KUBECONFIG: "{{ kubeconfig_path }}"
    PATH: "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

  tasks:
    # 1. Limpieza local
    - name: 🗑️ Borrar directorio temporal playbooks/files
      file:
        path: "{{ files_dir }}"
        state: absent

    # 2. Helm release
    - name: 🔍 ¿Existe release Traefik?
      shell: "{{ helm_bin_path }} status {{ traefik_release_name }} -n {{ traefik_namespace }}"
      register: helm_status
      failed_when: false
      changed_when: false

    - name: ❌ Desinstalar Helm release (si existe)
      shell: "{{ helm_bin_path }} uninstall {{ traefik_release_name }} -n {{ traefik_namespace }}"
      when: helm_status.rc == 0

    # 3. Job copy-wildcard-cert
    - name: ❌ Eliminar Job copy-wildcard-cert (si existe)
      shell: "{{ kubectl_bin_path }} delete job copy-wildcard-cert -n {{ traefik_namespace }} --ignore-not-found"

    # 4. Eliminar PVC bloqueado y su PV
    - name: 🔍 Obtener YAML del PVC certificados-longhorn
      shell: "{{ kubectl_bin_path }} get pvc {{ traefik_pvc_name }} -n {{ traefik_namespace }} -o json"
      register: pvc_json
      failed_when: false
      changed_when: false

    - name: 🧪 Verificar si el PVC tiene finalizers
      set_fact:
        pvc_has_finalizer: >-
          {{ '"finalizers"' in pvc_json.stdout and '"kubernetes.io/pvc-protection"' in pvc_json.stdout }}

    - name: 🧰 Quitar finalizers si el PVC está bloqueado
      when: pvc_has_finalizer
      shell: |
        {{ kubectl_bin_path }} patch pvc {{ traefik_pvc_name }} \
          -n {{ traefik_namespace }} \
          -p '{"metadata":{"finalizers":[]}}' --type=merge

    - name: ❌ Eliminar PVC certificados-longhorn (sin esperar)
      shell: "{{ kubectl_bin_path }} delete pvc {{ traefik_pvc_name }} -n {{ traefik_namespace }} --ignore-not-found"
      register: delete_pvc_result
      ignore_errors: true

    - name: ❌ Eliminar PV asociado (si existe)
      shell: >
        {{ kubectl_bin_path }} delete pv $({{ kubectl_bin_path }} get pv -o name |
        grep {{ traefik_pvc_name }} | cut -d/ -f2) --ignore-not-found
      when: delete_pvc_result.rc is not defined or delete_pvc_result.rc != 0
      ignore_errors: true

    # 5. Secretos (plano + Sealed)
    - name: ❌ Eliminar secretos/SealedSecrets
      shell: |
        {{ kubectl_bin_path }} delete secret       {{ traefik_dashboard_secret_name }} -n {{ traefik_namespace }} --ignore-not-found
        {{ kubectl_bin_path }} delete sealedsecret {{ traefik_dashboard_secret_name }} -n {{ traefik_namespace }} --ignore-not-found
        {{ kubectl_bin_path }} delete secret       {{ internal_tls_secret_name }}    -n {{ traefik_namespace }} --ignore-not-found
        {{ kubectl_bin_path }} delete sealedsecret {{ internal_tls_secret_name }}    -n {{ traefik_namespace }} --ignore-not-found

    # 6. CRDs Traefik v3
    - name: ❌ Eliminar CRDs Traefik v3 (si existen)
      shell: |
        for crd in ingressroutes ingressroutetcps ingressrouteudps \
                  middlewares tlsoptions tlsstores \
                  serverstransports serverstransporttcps traefikservices; do
          {{ kubectl_bin_path }} delete crd ${crd}.traefik.io --ignore-not-found
        done

    # 7. Service / NodePort
    - name: ❌ Eliminar Service traefik
      shell: "{{ kubectl_bin_path }} delete svc traefik -n {{ traefik_namespace }} --ignore-not-found"

    # 8. Confirmación final
    - name: ✅ Todo limpio
      debug:
        msg: "✅ Traefik, Job, PVC, PV, CRDs y secretos eliminados satisfactoriamente."
