# playbooks/uninstall-traefik.yml
# ========================================================
# 🧹 FASE FINAL: Desinstalación segura de Traefik
# - Elimina todos los recursos relacionados (Helm release, Secret, PVC, etc.)
# - Puede ejecutarse después de cualquier fase (1, 2 o 3)
# ========================================================

- name: 🧹 Desinstalar Traefik y limpiar recursos relacionados
  hosts: localhost
  gather_facts: false
  become: false

  vars_files:
    - ../vars/main.yml

  vars:
    kubeconfig_path: "/home/victory/.kube/config"
    helm_bin_path: "/usr/local/bin/helm"
    kubectl_bin_path: "/usr/local/bin/kubectl"
    env_path: "PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
    traefik_namespace: "kube-system"  # Cambia esto si usas otro namespace
    traefik_release_name: "traefik"   # Cambia esto si tienes otro nombre para tu release

  tasks:

    - name: ❌ Desinstalar Helm release de Traefik
      shell: >
        {{ helm_bin_path }} uninstall {{ traefik_release_name }} -n {{ traefik_namespace }}
      environment:
        PATH: "{{ env_path }}"
      ignore_errors: true

    - name: ❌ Eliminar SealedSecret del Dashboard (si existe)
      shell: >
        {{ kubectl_bin_path }} delete sealedsecret traefik-dashboard-secret -n {{ traefik_namespace }}
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      ignore_errors: true

    - name: ❌ Eliminar Secret plano del Dashboard (fallback)
      shell: >
        {{ kubectl_bin_path }} delete secret traefik-dashboard-secret -n {{ traefik_namespace }}
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      ignore_errors: true

    - name: ❌ Eliminar ConfigMap 'traefik-config' (si existe)
      shell: >
        {{ kubectl_bin_path }} delete configmap traefik-config -n {{ traefik_namespace }}
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      ignore_errors: true

    - name: ❌ Eliminar IngressRoute del Dashboard (si existe)
      shell: >
        {{ kubectl_bin_path }} delete ingressroute traefik-dashboard -n {{ traefik_namespace }}
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      ignore_errors: true

    - name: ❌ Eliminar PVCs creados por Traefik (si existen)
      shell: >
        {{ kubectl_bin_path }} delete pvc -n {{ traefik_namespace }} --selector=app.kubernetes.io/name=traefik
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      ignore_errors: true

    - name: ❌ Eliminar PVs huérfanos (si existen)
      shell: >
        {{ kubectl_bin_path }} delete pv --selector=app.kubernetes.io/name=traefik
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      ignore_errors: true

    - name: ❌ Eliminar almacenamiento local persistente (si está habilitado)
      shell: >
        rm -rf {{ traefik_storage_path }}
      when: traefik_storage_enabled | default(false) | bool
      ignore_errors: true

    - name: ✅ Verificar que no quedan recursos de Traefik
      shell: >
        {{ kubectl_bin_path }} get all -n {{ traefik_namespace }} || echo "✔️ Todos los recursos de Traefik han sido eliminados."
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"