# templates\secrets\tls-secret.yaml.j2
apiVersion: v1
kind: Secret
metadata:
  name: {{ secret_name }}
  namespace: {{ namespace }}
  annotations:
    sealedsecrets.bitnami.com/managed: "true"  # Opcional: útil si luego se convierte a SealedSecret
type: kubernetes.io/tls
data:
  tls.crt: {{ cert_base64 }}
  tls.key: {{ key_base64 }}